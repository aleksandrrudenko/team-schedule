# ✅ Чеклист деплоя в Railway

## Шаг 1: Проверка переменных окружения

Убедитесь, что в Railway Variables добавлены все переменные:

- [ ] `GOOGLE_CLIENT_ID` - из Google Cloud Console
- [ ] `GOOGLE_CLIENT_SECRET` - из Google Cloud Console  
- [ ] `SESSION_SECRET` - сгенерированная случайная строка
- [ ] `ALLOWED_USERS` - ваш email (например: `your-email@gmail.com`)
- [ ] `CALLBACK_URL` - `https://your-app.railway.app/auth/google/callback`
- [ ] `PORT` - `3000`
- [ ] `NODE_ENV` - `production`

## Шаг 2: Проверка Google OAuth настроек

В Google Cloud Console:

- [ ] OAuth 2.0 Client ID создан
- [ ] Authorized redirect URI добавлен: `https://your-app.railway.app/auth/google/callback`
- [ ] Google+ API или Google Identity API включен

## Шаг 3: Проверка деплоя в Railway

1. Откройте Railway Dashboard
2. Выберите ваш проект → ваш сервис
3. Перейдите в **Deployments** (вкладка)
4. Проверьте статус последнего деплоя:
   - ✅ **Success** - всё хорошо
   - ❌ **Failed** - проверьте логи

## Шаг 4: Проверка логов

1. В Railway Dashboard → ваш сервис
2. Перейдите в **Logs** (вкладка)
3. Проверьте, что нет ошибок:
   - ✅ `Server running on port 3000` - хорошо
   - ❌ `OAuth2Strategy requires a clientID` - переменные не установлены
   - ❌ `TypeError` - проверьте логи подробнее

## Шаг 5: Получение URL приложения

1. В Railway Dashboard → ваш сервис
2. Перейдите в **Settings** → **Domains**
3. Скопируйте домен (например: `team-schedule-production.railway.app`)
4. Ваш URL: `https://team-schedule-production.railway.app`

## Шаг 6: Тестирование

1. Откройте URL приложения в браузере
2. Должна произойти переадресация на Google для входа
3. Войдите с email, который указан в `ALLOWED_USERS`
4. После входа вы должны увидеть страницу с расписанием

## Возможные проблемы

### ❌ "OAuth2Strategy requires a clientID option"
**Решение:** Проверьте, что `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` добавлены в Railway Variables

### ❌ "Access denied"
**Решение:** 
- Проверьте, что ваш email точно указан в `ALLOWED_USERS`
- Email должен совпадать с тем, которым вы входите в Google

### ❌ "Redirect URI mismatch"
**Решение:**
- Проверьте, что `CALLBACK_URL` в Railway точно совпадает с URI в Google Cloud Console
- Используйте HTTPS (не HTTP)
- Проверьте, что нет лишних пробелов или символов

### ❌ Приложение не запускается
**Решение:**
- Проверьте логи в Railway
- Убедитесь, что все переменные добавлены
- Проверьте, что репозиторий подключен к Railway

## Готово! 🎉

Если всё работает, ваше приложение доступно по адресу:
`https://your-app.railway.app`
