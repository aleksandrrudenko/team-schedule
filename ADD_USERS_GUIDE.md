# Как добавить пользователей в whitelist

## 📋 Шаги для добавления пользователей

### 1. Откройте Railway Dashboard

1. Перейдите на https://railway.app/
2. Выберите ваш проект
3. Выберите ваш сервис (service)

### 2. Добапользователей в переменную ALLOWED_USERS

**ВАЖНО:** Переменные должны быть в **Service Variables**, а НЕ в **Shared Variables**!

#### Способ 1: Через отдельные переменные ALLOWED_USER_1, ALLOWED_USER_2, etc. (рекомендуется для Railway)

Railway может обрезать длинные значения переменных, поэтому **RECOMMENDED** использовать отдельные переменные:

1. Перейдите в **Variables** (вкладка сверху)
2. Нажмите **"+ New Variable"**
3. Создайте переменные:
   - `ALLOWED_USER_1` = `al.rudenko@playson.com`
   - `ALLOWED_USER_2` = `a.antipov@playson.com`
  **ALLOWED_USER_3` = `o.mylotskyi@playson.com`
   - `ALLOWED_USER_4` = `lavalauto@gmail.com`
   - И так далее для каждого пользователя

**Преимущества:**
- Railway не обрезает значения
- Легко добавлять/удалять отдельных пользователей
- Каждый email в отдельной переменной

#### Способ 2: Через Raw Editor (если ALLOWED_USERS работает)

1. Перейдите в **Variables** (вкладка сверху)
2. Нажмите **"Raw Editor"**
3. Найдите строку с `ALLOWED_USERS`
4. Добавьте email адреса через запятую:

```
ALLOWED_USERS=al.rudenko@playson.com,a.antipov@playson.com,новый-пользователь@playson.com
```

**Пример:**
```
ALLOWED_USERS=al.rudenko@playson.com,a.antipov@playson.com,user3@playson.com,user4@playson.com
```

5. Нажмите **"Save"**

#### Способ 2: Через отдельные переменные

1. Перейдите в **Variables** (вкладка сверху)
2. Найдите переменную `ALLOWED_USERS`
3. Нажмите на неё для редактирования
4. В поле **Value** добавьте email адреса через запятую:

```
al.rudenko@playson.com,a.antipov@playson.com,новый-пользователь@playson.com
```

5. Нажмите **"Save"**

### 3. Проверьте, что переменная в правильном месте

**✅ Правильно:** Переменная должна быть в **Service Variables** (для конкретного сервиса)

**❌ Неправильно:** Переменная в **Shared Variables** (общие для всех сервисов)

Если переменная в Shared Variables:
1. Удалите её оттуда
2. Добавьте в Service Variables (для вашего сервиса)

### 4. Перезапустите сервис

После изменения переменных:
1. Перейдите в **Deployments** (вкладка сверху)
2. Найдите последний deployment
3. Нажмите **"Redeploy"** или подождите автоматического перезапуска

### 5. Проверьте логи

После перезапуска проверьте логи в Railway:

1. Перейдите в **Deployments**
2. Откройте последний deployment
3. Посмотрите логи при старте

Должно быть видно:

```
🔐 Whitelist configuration:
   ALLOWED_USERS env (raw): "al.rudenko@playson.com,a.antipov@playson.com,user3@playson.com"
   Parsed whitelist: [ 'al.rudenko@playson.com', 'a.antipov@playson.com', 'user3@playson.com' ]
   Whitelist count: 3
   Whitelist emails:
      [1] "al.rudenko@playson.com"
      [2] "a.antipov@playson.com"
      [3] "user3@playson.com"
```

### 6. Проверьте доступ

Попросите пользователя:
1. Открыть приложение в инкогнито режиме (чтобы избежать кеша)
2. Попробовать войти через Google OAuth
3. Проверить логи в Railway - там будет видно, почему доступ был разрешен или запрещен

## 🔍 Диагностика проблем

### Проблема: Пользователь не может войти

**Проверьте логи в Railway при СТАРТЕ сервера:**

Должно быть видно:

```
🔐 Whitelist configuration:
   ALLOWED_USERS env (raw): "al.rudenko@playson.com,a.antipov@playson.com,lavalauto@gmail.com"
   Parsed whitelist: [ 'al.rudenko@playson.com', 'a.antipov@playson.com', 'lavalauto@gmail.com' ]
   Whitelist count: 3
   Whitelist emails:
      [1] "al.rudenko@playson.com"
      [2] "a.antipov@playson.com"
      [3] "lavalauto@gmail.com"
```

**Если видите только одного пользователя в логах:**

```
🔐 Whitelist configuration:
   ALLOWED_USERS env (raw): "al.rudenko@playson.com"
   Parsed whitelist: [ 'al.rudenko@playson.com' ]
   Whitelist count: 1
```

**Это значит:**
- Переменная не была правильно сохранена в Railway
- ИЛИ сервер не перезапустился после изменения переменных
- ИЛИ переменная была добавлена в Shared Variables вместо Service Variables

**Решение:**

1. **Проверьте, что переменная в Service Variables (не Shared Variables)**
   - Railway Dashboard → ваш сервис → Variables (НЕ Shared Variables!)
   - Убедитесь, что переменная `ALLOWED_USERS` находится там

2. **Проверьте формат переменной в Railway**
   - Откройте переменную `ALLOWED_USERS` для редактирования
   - Убедитесь, что все email адреса в одной строке через запятую:
     ```
     al.rudenko@playson.com,a.antipov@playson.com,o.mylotskyi@playson.com,lavalauto@gmail.com
     ```
   - **ВАЖНО:** Убедитесь, что нет лишних запятых в конце строки
   - **ВАЖНО:** Убедитесь, что нет переносов строк внутри значения

3. **Сохраните переменную и перезапустите сервис**
   - Нажмите галочку (✓) для сохранения
   - Railway должен автоматически перезапустить сервис
   - ИЛИ сделайте Redeploy вручную: Deployments → последний deployment → Redeploy

4. **Проверьте логи после перезапуска**
   - После перезапуска проверьте логи при старте
   - Должно быть видно всех пользователей в whitelist

**При попытке входа должно быть видно:**

```
🔍 Authentication attempt:
   User email: lavalauto@gmail.com
   User email (lowercase): lavalauto@gmail.com
   Whitelist: [ 'al.rudenko@playson.com', 'a.antipov@playson.com', 'lavalauto@gmail.com' ]
   Whitelist check (exact): false
   Whitelist check (lowercase): true
```

Если `Whitelist check (lowercase): false`, значит email не найден в whitelist.

**Возможные причины:**

1. **Email не добавлен в ALLOWED_USERS**
   - Проверьте, что email точно совпадает (регистр не важен, но символы должны совпадать)
   - Проверьте, нет ли опечаток

2. **Переменная в Shared Variables вместо Service Variables**
   - Удалите из Shared Variables
   - Добавьте в Service Variables

3. **Сервис не перезапустился после изменения переменных**
   - Сделайте Redeploy вручную

4. **Неправильный формат переменной**
   - Используйте запятые: `email1@domain.com,email2@domain.com`
   - Без кавычек: `ALLOWED_USERS=email1@domain.com,email2@domain.com`
   - Не используйте: `ALLOWED_USERS="email1@domain.com,email2@domain.com"`
   - **Убедитесь, что все email в одной строке (без переносов строк)**

### Проблема: В логах видно только одного пользователя

**Проверьте формат:**

✅ Правильно:
```
ALLOWED_USERS=al.rudenko@playson.com,a.antipov@playson.com
```

❌ Неправильно:
```
ALLOWED_USERS="al.rudenko@playson.com,a.antipov@playson.com"
ALLOWED_USERS=al.rudenko@playson.com; a.antipov@playson.com
```

## 📝 Примеры форматов

### ✅ Правильные форматы:

```
# Через запятую (рекомендуется)
ALLOWED_USERS=al.rudenko@playson.com,a.antipov@playson.com,user3@playson.com

# Через запятую с пробелами (пробелы автоматически удалятся)
ALLOWED_USERS=al.rudenko@playson.com, a.antipov@playson.com, user3@playson.com
```

### ❌ Неправильные форматы:

```
# С кавычками
ALLOWED_USERS="al.rudenko@playson.com,a.antipov@playson.com"

# С точкой с запятой
ALLOWED_USERS=al.rudenko@playson.com;a.antipov@playson.com

# С переносами строк (в одной переменной)
ALLOWED_USERS=al.rudenko@playson.com
a.antipov@playson.com
```

## ⚠️ Важные замечания

1. **Регистр не важен** - `User@Domain.com` и `user@domain.com` считаются одинаковыми
2. **Пробелы автоматически удаляются** - можно использовать `email1@domain.com, email2@domain.com`
3. **После изменения переменных нужно перезапустить сервис** - Railway обычно делает это автоматически, но можно сделать вручную через Redeploy
4. **Переменная должна быть в Service Variables**, а не в Shared Variables

## 🆘 Если ничего не помогает

1. Проверьте логи Railway при старте сервиса - там должно быть видно, какие пользователи в whitelist
2. Проверьте логи при попытке входа - там будет видно, почему доступ был запрещен
3. Убедитесь, что пользователь использует правильный Google аккаунт (тот, email которого добавлен в whitelist)
4. Попросите пользователя попробовать в инкогнито режиме (чтобы избежать проблем с кешем)
