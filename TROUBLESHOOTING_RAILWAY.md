# 🔧 Решение проблемы: Missing required environment variables

## Проблема

В логах видна ошибка:
```
❌ ERROR: Missing required environment variables!
   Required: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
```

Это означает, что Railway не видит переменные окружения.

## ✅ Решение

### Шаг 1: Проверьте, что переменные добавлены в правильном месте

В Railway есть два уровня для переменных:
- **Project level** (уровень проекта) - применяется ко всем сервисам
- **Service level** (уровень сервиса) - применяется только к конкретному сервису

**Важно:** Переменные должны быть добавлены на уровне **SERVICE** (сервиса), а не проекта!

### Шаг 2: Как правильно добавить переменные

1. Откройте Railway Dashboard: https://railway.app/
2. Выберите ваш **проект**
3. Выберите ваш **сервис** (service) - тот, который запускает приложение
4. Перейдите в **Variables** (вкладка сверху)
5. Убедитесь, что вы видите список переменных для **этого сервиса**

### Шаг 3: Проверьте названия переменных

Названия должны быть **точными**, без опечаток:

✅ **Правильно:**
- `GOOGLE_CLIENT_ID`
- `GOOGLE_CLIENT_SECRET`
- `SESSION_SECRET`
- `ALLOWED_USERS`
- `CALLBACK_URL`
- `PORT`
- `NODE_ENV`

❌ **Неправильно:**
- `GOOGLE_CLIENT_ID_` (лишний символ)
- `google_client_id` (маленькие буквы)
- `GOOGLE_CLIENT_ID ` (пробел в конце)
- `GOOGLE_CLIENT_ID` (другой регистр)

### Шаг 4: Добавьте переменные заново

Если переменные уже есть, но не работают:

1. Удалите все переменные (или проверьте их значения)
2. Добавьте заново через **"+ New Variable"**:

```
Name: GOOGLE_CLIENT_ID
Value: ваш-client-id-из-google-cloud-console
```

3. Повторите для всех переменных

### Шаг 5: Используйте Raw Editor (рекомендуется)

1. В Railway Variables нажмите **"Raw Editor"**
2. Вставьте все переменные в формате:

```
GOOGLE_CLIENT_ID=ваш-client-id
GOOGLE_CLIENT_SECRET=ваш-client-secret
SESSION_SECRET=ваш-session-secret
ALLOWED_USERS=ваш-email@gmail.com
CALLBACK_URL=https://ваш-домен.railway.app/auth/google/callback
PORT=3000
NODE_ENV=production
```

3. **Важно:** 
   - Каждая переменная на новой строке
   - Формат: `NAME=value` (без пробелов вокруг `=`)
   - Нет кавычек вокруг значений
   - Нет пустых строк между переменными

4. Нажмите **"Save"**

### Шаг 6: Перезапустите деплой

После добавления переменных:

1. Railway должен автоматически перезапустить деплой
2. Если не перезапустился - нажмите **"Redeploy"** в разделе Deployments
3. Или измените любую переменную (добавьте пробел и удалите) чтобы триггернуть перезапуск

### Шаг 7: Проверьте логи

1. После перезапуска проверьте логи
2. Ищите сообщение: `Server running on port 3000`
3. Если видите ошибку - проверьте значения переменных

## 🔍 Частые ошибки

### Ошибка 1: Переменные на уровне проекта, а не сервиса
**Решение:** Убедитесь, что переменные добавлены в разделе Variables **вашего сервиса**, а не проекта

### Ошибка 2: Опечатки в названиях
**Решение:** Скопируйте названия переменных точно из инструкции

### Ошибка 3: Пустые значения
**Решение:** Убедитесь, что все переменные имеют значения (не пустые)

### Ошибка 4: Пробелы в значениях
**Решение:** Убедитесь, что нет лишних пробелов в начале или конце значений

### Ошибка 5: Кавычки в значениях
**Решение:** Не используйте кавычки в Raw Editor (Railway добавит их автоматически)

## ✅ Проверка

После исправления в логах должно быть:

```
✅ Server running on port 3000
```

Вместо:

```
❌ ERROR: Missing required environment variables!
```

## 📞 Если не помогло

1. Проверьте, что вы добавили переменные в **правильный сервис**
2. Убедитесь, что названия переменных **точные** (скопируйте из инструкции)
3. Попробуйте удалить все переменные и добавить заново через Raw Editor
4. Проверьте, что значения не пустые
